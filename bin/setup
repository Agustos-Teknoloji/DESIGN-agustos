#!/usr/bin/env bash
# One-time setup for a fresh clone or worktree.
# Activates the pre-push hook in .githooks/, which runs scripts/ci.sh.

set -euo pipefail

cd "$(dirname "${BASH_SOURCE[0]}")/.."

git config core.hooksPath .githooks
echo "Git hooks active: core.hooksPath = .githooks"
